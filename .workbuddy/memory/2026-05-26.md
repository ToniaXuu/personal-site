# 2026-05-26

## 修复 personal-site 三个 Bug

### Bug 1：深色主题首次加载白色闪现
- **根因**：`useTheme.ts` 只在 `onMounted()` 中应用 `dark` class，在此之前页面已按浅色主题渲染
- **修复**：在 `nuxt.config.ts` 的 `app.head.script` 中注入阻塞内联脚本，在 DOM 渲染前读取 localStorage 并立即添加 `dark` class
- 修改文件：`nuxt.config.ts`

### Bug 2：浅色主题下博客代码块看不清
- **根因**：`--prose-pre-bg` 在浅色模式下设为 `rgba(0,0,0,0.03)`（几乎白色），且未定义前景色
- **修复**：
  - 浅色模式 `pre` 块改为深色背景 `#1e293b` + 浅色文字 `#e2e8f0`
  - 新增 `--prose-pre-text`、`--prose-code-bg`、`--prose-blockquote-bg`、`--prose-table-border`、`--prose-th-bg` 等变量
  - `.blog-prose` 内显式定义 `pre`、`code`、`blockquote`、`table` 样式，使用 `!important` 确保优先级
- 修改文件：`assets/css/main.css`

### Bug 3：博客页面无过渡动画 / 点击后反应慢
- **修复**：
  - `app.vue`：添加 `<NuxtLoadingIndicator>` 全局导航进度条 + `<NuxtPage>` 页面过渡动画（fade+slide）
  - `pages/blog/index.vue`：添加骨架屏加载态，使用 `lazy: true` 实现客户端即时渲染
  - `pages/blog/[...slug].vue`：添加骨架屏加载态 + 404 模板处理，使用 `lazy: true`，SSR 404 用 `import.meta.server` 判断
- 修改文件：`app.vue`、`pages/blog/index.vue`、`pages/blog/[...slug].vue`

## 构建验证
- `nuxt build` 成功，零错误

---

## 主题改版与内容更新

### 1. 添加头像
- 头像图片复制到 `public/avatar.jpg`
- `HeroSection.vue`：标题上方添加圆形头像展示
- `AboutSection.vue`：替换原来的字母 "T" 为真实头像

### 2. 主题颜色改为微紫和浅绿
- **浅色主题**：green-500 (#22c55e) + violet-500 (#8b5cf6)
- **深色主题**：green-400 (#4ade80) + violet-400 (#a78bfa)
- 修改范围：`main.css` 全部 CSS 变量、`20+ 个 Vue 组件`、粒子背景颜色数组
- 涉及：按钮渐变、图标颜色、标签边框、 glow 效果、进度条、导航下划线等

### 3. 去除公司信息
- `TimelineSection.vue`：移除全部 `company` 字段
- `pages/detail/experience/[id].vue`：同步移除公司数据，去除公司名称展示

### 4. 区分工作项目和个人项目
- `ProjectsSection.vue`：每个项目添加 `type: 'work' | 'personal'` 字段
- 项目卡片右上角显示标签：绿色"工作"/紫色"个人"
- `pages/detail/project/[id].vue`：同步添加类型标签展示

### 5. 头部菜单选中效果
- `AppNavbar.vue`：导航链接增加 `px-3 py-1.5 rounded-lg` 内边距和圆角
- `main.css` `.nav-link`：
  - 选中时添加 `background: var(--accent-glow)` 淡淡背景
  - hover 时添加 `background: var(--glass-hover)`
  - 下划线改为圆角、位置调整为在文字下方居中

### 构建验证
- `nuxt build` 成功，零错误
- 已推送至 GitHub：`8c7c1ff..808096f`
